#include "yei_driver.h"

yei_driver::yei_driver() {



}

int yei_driver::send() {
    //int fd = open("/dev/i2c", O_RDWR);
    //ioctl(fd, IOCTL_COMMAND, args);
    //size = write(ctx->s, req, req_length);
}
